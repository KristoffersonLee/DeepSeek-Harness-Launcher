# 构建产物 / Build artifacts
*.exe
*.old
*.old2
*.old3
*.old4
selftest.log
setup.log
setup.detect.log

# 根目录的 WebView2 运行库副本（由 build.ps1 从 lib\ 拷贝，入库以 lib\ 为准）
/Microsoft.Web.WebView2.Core.dll
/Microsoft.Web.WebView2.WinForms.dll
/WebView2Loader.dll

# 敏感配置：仓库根与 %APPDATA% 的 .env 可存放 DSH_LAN_PIN 等凭据，禁止入库
.env
.env.*

# 开发期误产物（如 PowerShell 重定向 `> 0` 生成的 “0” 文件）
0

# WorkBuddy 等工具运行时目录（会按需重建，不入库）
.workbuddy/

# 编辑器与系统 / Editor & OS
.vs/
.vscode/
*.user
*.suo
Thumbs.db
Desktop.ini

# Node
node_modules/
npm-debug.log*
